# Environnement applicatif
NODE_ENV=development
PORT=3000
LOG_LEVEL=debug

# CORS — origines autorisées séparées par virgules
CORS_ORIGINS=http://localhost:3001

# Base de données PostgreSQL (à renseigner dès S1)
# DATABASE_URL=postgresql://mytdfrik:changeme@localhost:5432/mytdfrik

# Redis (à renseigner dès S1)
# REDIS_URL=redis://localhost:6379

# Sentry (optionnel en dev, requis en production)
# SENTRY_DSN=
